import type { WebMcpTool } from "@vedaxi/contracts";
import type { VideoEvidenceService } from "./service";
const schema = { type: "object", properties: { query: { type: "string", maxLength: 160, pattern: "\\S" } }, required: ["query"], additionalProperties: false };
function queryInput(input: unknown): string { if (!input || typeof input !== "object" || Array.isArray(input)) throw new Error("input must contain only query"); const f = input as Record<string, unknown>; if (Object.keys(f).length !== 1 || typeof f.query !== "string" || !f.query.trim() || f.query.length > 160) throw new Error("query must be a non-blank string of at most 160 characters"); return f.query; }
export function createVideoSearchTool(service: VideoEvidenceService): WebMcpTool { return { name: "search_video_evidence", title: "Search video evidence", description: "Search this publisher's transcript for exact evidence and provenance only.", inputSchema: schema, annotations: { readOnlyHint: true, untrustedContentHint: true }, execute: async (input) => service.search(queryInput(input)) }; }
export function createVideoTranscriptTool(service: VideoEvidenceService): WebMcpTool { return { name: "read_video_transcript", title: "Read video transcript", description: "Read this publisher's transcript cues without analysis.", inputSchema: { type: "object", properties: {}, additionalProperties: false }, annotations: { readOnlyHint: true, untrustedContentHint: true }, execute: async (input) => { if (!input || typeof input !== "object" || Array.isArray(input) || Object.keys(input as object).length) throw new Error("input must be empty"); return service.readTranscript(); } }; }
