# M0B Reconstructed Historical Narrative — 2026-08-30T20:18:06Z

This file preserves the first run's author-provided reconstruction. It is **not raw exit evidence** and is superseded for M0 exit by the artifact-bound rerun under `docs/evidence/M0/raw/`. Its observations remain historical context only.

## Environment

- Base commit: `d31ca363f35e537aedc9a8c1528bf3bc618dcfcd`
- Working tree: uncommitted M0B changes present
- Operating system: Windows
- Client: Codex In-app Browser
- Build flavor: production
- Exact client version/build: unavailable from the runtime (`BLOCKED`)
- UTC run timestamp: `2026-08-30T20:18:06Z`
- Local run timestamp: `2026-08-31T01:48:12+05:30`
- Ephemeral session IDs: intentionally not retained

## Simultaneous-frame attempt

The paper publisher opened at `http://localhost:4173`. Its page reported a secure context and native registration state `active`. The page visibly embedded the independent video publisher at `http://localhost:4174` using the declared `tools` iframe permission.

The framed video publisher reported a secure context but native registration state `unsupported`. A fresh inventory exposed only the paper capability. The simultaneous two-origin frame hypothesis was therefore tested and **FAILED**. This is a failure, not an evidence block.

## Clean-room sequential run

Sub Agent 7 received only this generic task:

> Find the paper publisher’s final analyzed-sample statement and the independent author-video publisher’s calibration-exclusion statement. Return each source’s own evidence and the origin reported by the browser. Do not compare the statements or infer a conclusion.

The prompt contained no publisher tool names.

At the paper origin, the agent dynamically discovered `read_paper_probe_evidence` and invoked it with:

```json
{"query":"final analyzed-sample statement"}
```

The native tool returned only the paper evidence object with score `3`: stable ID `probe.paper.methods.final-analysis`, origin `http://localhost:4173`, locator `Methods, participants`, the exact forty-participant excerpt, and `Protocol probe paper methods passage` provenance.

The agent then navigated to the visible independent video URL, `http://localhost:4174`. As a top-level document, the video publisher reported native state `active`. The agent dynamically discovered `read_video_probe_evidence` and invoked it with:

```json
{"query":"calibration-exclusion statement"}
```

The native tool returned only the video evidence object with score `2`: stable ID `probe.video.transcript.calibration-drift`, origin `http://localhost:4174`, locator `00:03:12`, the exact calibration-removal excerpt including “did not replace them,” and `Protocol probe video transcript at 00:03:12` provenance.

The clean-room report explicitly recorded no DOM substitution. It returned the two source-owned results and origins without comparing them, labeling a contradiction, or deriving `34`. The sequential two-origin hypothesis therefore **PASSED**.

## Registration lifecycle run

The root browser run disabled the directly opened video registration. A fresh browser observation reported zero tools while the human transcript remained visible. Re-enable returned the video registration to `active`.

The run then disabled the paper registration. A fresh browser observation reported zero tools while the human paper evidence and embedded frame remained visible.

Tool-list changes were observed through browser notifications. No direct `toolchange` event counter was installed, so the evidence does not claim an event count or event timing.

No console errors were observed.

## Evidence limitations

- Exact client version/build was unavailable from the runtime: `BLOCKED`.
- Screenshots were not captured: `BLOCKED` as an evidence form.
- Raw artifacts from this first run were not retained. The later artifact-bound rerun supplies the exit trace.
- This run observed an H9 removal-feasibility precursor only; full H9 was not evaluated because no persisted note existed.
- The run was local and does not prove deployed-client parity.
- The iframe's `unsupported` result means simultaneous discovery failed even though the frame was secure and declared `allow="tools"`.
- The successful sequential route proves a topology for subsequent modules; it does not prove the final agent derivation, human confirmation, persistence, Semantic Stage, or submission package.

## Decision

Selected topology: **sequential**.

M1 and M2 must remain true independent origins. The same external task must visit each origin, dynamically discover its publisher capability, retain provenance, and return to the paper publisher for later composition. No aggregator, copied video result, simulated simultaneous inventory, or DOM substitute is permitted.
